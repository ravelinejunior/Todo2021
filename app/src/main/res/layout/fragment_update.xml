<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="16dp"
    android:orientation="vertical"
    tools:context=".presentation.ui.fragment.UpdateFragment">

    <EditText
        android:id="@+id/editTextFragmentUpdateTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:autofillHints="no"
        android:background="@drawable/custom_input"
        android:ems="10"
        android:fontFamily="@font/lato"
        android:hint="@string/t_tulo"
        android:inputType="text"
        android:minHeight="50dp"
        android:padding="8dp"
        android:paddingStart="12dp"
        android:paddingEnd="8dp"
        android:textColorHint="@color/darkGray"
        tools:ignore="TextContrastCheck" />

    <Spinner
        android:id="@+id/spinnerFragmentUpdatePriority"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:dropDownSelector="@drawable/ic_baseline_arrow_downward_24"
        android:entries="@array/priorities"
        android:fontFamily="@font/lato"
        android:paddingStart="4dp"
        android:paddingEnd="8dp"
        android:spinnerMode="dropdown"
        tools:ignore="SpeakableTextPresentCheck" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <EditText
            android:id="@+id/editTextTextMultiLineFragmentUpdateDescription"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="8dp"
            android:autofillHints="yes"
            android:background="@drawable/custom_input"
            android:ems="10"
            android:imeOptions="actionDone"
            android:fontFamily="@font/lato"
            android:gravity="start|top"
            android:hint="@string/descri_o"
            android:inputType="textMultiLine"
            android:padding="16dp"
            android:textColorHint="@color/darkGray"

            />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabFragmentUpdate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            android:clickable="true"
            android:contentDescription="@string/app_name"
            android:focusable="true"
            android:src="@drawable/ic_validade"
            app:backgroundTint="@color/purple_200"
            app:tint="@color/white" />
    </RelativeLayout>


</LinearLayout>